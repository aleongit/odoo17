<?xml version="1.0"?>
<odoo>

    <!-- list -->
    <record id="estate.property_tag_list" model="ir.ui.view">
        <field name="name">Property Tag List</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" string="Name" />
            </tree>
        </field>
    </record>

    <!-- form -->
    <record id="estate.property_tag_form" model="ir.ui.view">
        <field name="name">Property Tag Form</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <form string="Test">
                <sheet>
                    <h1>
                        <field name="name"/>
                    </h1>
                </sheet>
            </form>
        </field>
    </record>

    <!-- search -->
    <record id="estate.property_tag_search" model="ir.ui.view">
        <field name="name">estate.property.tag.search</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <search string="Busca...">
                <field name="name" string="Tag" />
            </search>
        </field>
    </record>


</odoo>